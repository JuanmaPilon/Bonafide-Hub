# @bonafide/shared

Placeholder para el paquete compartido entre apps (tipos, validaciones, servicios reutilizables).

> **Estado**: actualmente sin uso. Se mantiene trackeado para que el directorio `packages/` exista en el checkout de CI (verificación de estructura mínima del repo).

## Uso futuro planeado

1. Tipos compartidos (ej. `XpRoleRule`, `XpConfig`, `GuildChannel`).
2. Validaciones reutilizables.
3. Servicios de negocio compartidos entre bot, API y web.
